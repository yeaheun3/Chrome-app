// h2 태그 자식이 #clock이므로 공백을 주지 않음
const clock = document.querySelector("h2#clock");

/* interval : 함수가 특정한 시간마다 반복해서 실행되게 하고 싶을 때 쓰는 것
function sayHello() {
  console.log("Hello");
}

인자값으로 호출하려는 함수 이름, 호출 사이 시간(밀리초) 써주기
setInterval(sayHello, 5000);
*/
